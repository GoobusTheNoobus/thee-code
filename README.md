# TheeCode
TheeCode is a coding language that is very bad.

# Building/Running
Make sure you are at project root. Then, run `make`. You have to have g++ compiler to work. After making the file, a file theecode.exe will appear in your current directory.
To run, use `./theecode.exe`.

# Developers
- @GoobusTheNoobus